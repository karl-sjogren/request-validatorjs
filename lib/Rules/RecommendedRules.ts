import {Required} from "./Required";
import {Max} from "./Max";
import {Min} from "./Min";
/**
 * Exports the recommended rules in a bundle to ease import lines.
 */
export {
    Required,
    Max,
    Min,
}